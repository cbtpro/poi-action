spring:
  config:
    import: optional:classpath:application-logging-dev.yml
    activate:
      on-profile: dev

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: health,info,metrics,env,beans
  endpoint:
    health:
      show-details: always

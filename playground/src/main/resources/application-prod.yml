spring:
  config:
    import: optional:classpath:application-logging-prod.yml
    activate:
      on-profile: prod

management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        exclude: "*"
  endpoint:
    health:
      show-details: when_authorized
